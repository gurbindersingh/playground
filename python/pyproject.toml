[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "imagehash>=4.3.2",
    "pillow>=12.2.0",
    "pytest-playwright>=0.7.0",
]

[project.scripts]
image-hashing = "image_hashing:main"
